{
	"version": "0.2.0",
	"configurations": [
		{
			"type": "mock",
			"request": "launch",
			"name": "Debug Archetype (simple)",
			"program": "${file}",
		},
		{
			"type": "mock",
			"request": "launch",
			"name": "Debug Archetype (all parameters)",
			"program": "${file}",
			"args": [	"--all" ]
		}
	]
}